# Backend API URL (no trailing slash)
# For development with Vite dev server:
VITE_API_URL=http://localhost:8080/api/v1
# For production behind Nginx proxy (default, relative path):
# VITE_API_URL=/api/v1
